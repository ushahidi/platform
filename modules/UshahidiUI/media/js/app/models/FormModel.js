define(["jquery", "backbone"],
	function($, Backbone) {
		var FormModel = Backbone.Model.extend(
		{
			initialize : function() {
	
			},

			defaults :
			{
	
			},

			validate : function(attrs) {
	
			}
		});
	
		return FormModel;
	}); 