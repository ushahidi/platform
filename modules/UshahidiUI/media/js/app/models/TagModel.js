define(["jquery", "backbone"],
	function($, Backbone) {
		var TagModel = Backbone.Model.extend(
		{
			initialize : function() {
	
			},

			defaults :
			{
	
			},

			validate : function(attrs) {
	
			}
		});
	
		return TagModel;
	}); 